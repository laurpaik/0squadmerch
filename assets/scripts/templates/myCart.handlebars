<table>
  <thead>
    <tr>
      <th>Item</th>
      <th>Description</th>
      <th>Price</th>
      <th>Quantity</th>
    </tr>
  </thead>

  <tbody>
  {{#each order.items as |item|}}
    <tr>
      <td>{{item.name}}</td>
      <td>{{item.description}}</td>
      <td>{{item.price}}</td>
      <td>{{item.quantity}}</td>
      <td>
        <button class="btn btn-primary item-edit" data-id="{{item.id}}" >Edit</button>
        <button class="btn btn-danger item-delete" data-id="{{item.id}}">Delete</button>
      </td>
    </tr>
    {{/each}}
  </tbody>
</table>
